// DEPRECATED — Supabase foi substituído por Neon.
// Use `lib/neon.ts` → `sql` pra queries.
// Mantido como stub vazio pra evitar erro caso algo ainda importe.
export function getSupabaseAdmin(): null {
  return null;
}
