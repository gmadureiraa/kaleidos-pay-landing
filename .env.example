# Supabase (waitlist storage)
# Cria um projeto em https://supabase.com e cola a URL e a service_role key.
# Se estas não estiverem setadas, a landing continua funcionando, mas os emails
# só aparecem nos logs do servidor (fallback gracioso).
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
